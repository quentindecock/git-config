Installation
------------

To install, run

    git clone git://github.com/quentindecock/git-config.git ~/.git-config/
    cd ~/.git-config
    make install

To update the repository, run

    make update
